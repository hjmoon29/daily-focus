# Daily Focus

할 일 · 뽀모도로 · 오늘 메모 통합 대시보드 (PWA)

## 로컬 실행
```bash
node server.js
```
표시되는 `http://<LAN-IP>:8080` 주소로 접속.

## 배포
GitHub Pages에서 자동 호스팅됩니다. `main` 브랜치의 루트를 서빙.

## 폰에 설치 (PWA)
1. 폰 브라우저(Chrome/Safari)에서 배포 URL 접속
2. **홈 화면에 추가** 선택
3. 홈 화면에서 앱처럼 실행
